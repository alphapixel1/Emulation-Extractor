<Window x:Class="Emulation_Extractor.FileListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Emulation_Extractor"
        mc:Ignorable="d"
        Title="Scanned Files" Width="733"
        ResizeMode="CanResize" Height="376" Loaded="Window_Loaded"
        >
    <Grid Background="#FF2D2D2D">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ListView Grid.Row="1" x:Name="FileListView" ItemsSource="{Binding GameFiles}" Background="White" d:ItemsSource="{d:SampleData ItemCount=5}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="File Name" Width="220" DisplayMemberBinding="{Binding NameNoExtension}"></GridViewColumn>
                    <GridViewColumn Header="Emulator" Width="120" DisplayMemberBinding="{Binding EmulatorName}"></GridViewColumn>
                    <GridViewColumn Header="Path" DisplayMemberBinding="{Binding FilePath}"/>
                </GridView>
            </ListView.View>
            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate DataType="GameFile">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontWeight="Bold" FontSize="14" Text="{Binding Name}"/>
                                
                                <TextBlock Text="{Binding ItemCount}" Margin="5,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                    <GroupStyle.ContainerStyle>
                        <Style TargetType="{x:Type GroupItem}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate>
                                        <Expander IsExpanded="True">
                                            <Expander.Header>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock FontWeight="Bold" FontSize="14" Text="{Binding Name}"/>
                                                    <TextBlock Text="{Binding ItemCount}" Margin="5,0" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Expander.Header>
                                            <ItemsPresenter />
                                        </Expander>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GroupStyle.ContainerStyle>
                </GroupStyle>
            </ListView.GroupStyle>
        </ListView>
        <Grid Grid.Row="2" Height="28px">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Margin="5,2">Go</Button>
            <Button Grid.Column="1" Margin="5,2">Cancel</Button>
        </Grid>
    </Grid>
    
</Window>
